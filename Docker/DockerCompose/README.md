# Docker Compose

## Basic Docker Compose
```bash
# Create Docker Compose
docker compose up

# Create Docker Compose with Detach Mode
docker compose up -d

# Docker Compose with building the Image
docker compose up --build
```