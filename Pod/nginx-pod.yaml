apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-pod
    env: prod
  name: nginx-pod
  namespace: default
spec:
  containers:
  - image: nginx
    name: nginx-pod
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always